# P12 Runtime Integration Status

**Phase:** FP12 / P12  
**Status:** Local runtime checks verified; external P12 exit gate remains unverified
**Updated:** 13 September 2026

## Current position

The repository has executable migrations, a durable PostgreSQL-outbox + Redis/BullMQ worker baseline, and local runtime evidence. Runtime certification remains blocked on external environment evidence that cannot be generated from a workstation.

### Worker baseline

- PostgreSQL `OutboxEvent` source of truth;
- concurrent claiming with `FOR UPDATE SKIP LOCKED`;
- stale PROCESSING lease reclamation;
- bounded retry and FAILED visibility;
- deterministic eventId BullMQ job identity;
- notification consumer with dedupe-key persistence;
- missing referenced aggregates fail closed;
- scheduled inventory-reservation expiry using PostgreSQL authority;
- graceful shutdown and queue draining;
- controlled failed-outbox replay with explicit financial replay confirmation;
- bounded, audited Redis-loss reconstruction for idempotent notification jobs only.

### Reproducibility and database evidence

The root `pnpm-lock.yaml` was generated by pnpm 11.25.0 and CI uses `pnpm install --frozen-lockfile`.

`packages/database/prisma/migrations/` contains an initial schema migration and integrity migration. Both applied cleanly to a disposable PostgreSQL database before the full integration suite, and an isolated backup/restore rehearsal preserved business invariants.

### Local evidence state

On 13 September 2026, local noncached lint, typecheck, documentation-link and secret checks, production build, and all 12 workspace test tasks passed. The suite used a newly created migrated PostgreSQL database and Redis; it includes PostgreSQL concurrency and Redis/BullMQ worker integration. Browser verification covered 390px route layouts, same-origin registration cookies, and fragment-only password-reset/verification links.

The dependency audit is also clean after upgrading Swagger UI and Vitest and locking patched transitive `deepmerge-ts`, `js-yaml`, and `mysql2` versions.

`pnpm p12:preflight` passes. `pnpm p12:gate` remains intentionally failing because `artifacts/p12/gate.json` must contain real staging, provider, recovery, and UAT evidence.

## Remaining executable sequence

1. Deploy the Render preview blueprint with its managed PostgreSQL and Key Value services.
2. Configure provider sandbox credentials and a verified sender/domain.
3. Run staging smoke, provider-failure, worker-recovery, accessibility, and performance exercises.
4. Record human UAT and recovery evidence in `artifacts/p12/gate.json`.
5. Pass `pnpm p12:gate` before P13.
